# 导航定稿、PBL 规划页与主分支兼容

> 后续更新：2026-09-27 已将“小工具工坊”导航更名为“创意工坊”，原工具制作功能移入实验室；手机 PBL 仍放在工具菜单。当前版本与提交说明见 [本轮记录](creative-workshop-achievements-20260927.md)，下方保留上一轮验收记录。

## 基线与范围

- 当前分支：`feat/home-copy-polish-20260926`
- 已从 `c7a56f5` 快进到本次核验的 `origin/main`：`af039c0`
- 包含新合入的 #121、#123、#124、#125、#126；保留原有未提交的首页、个人空间、牧场、设置、导引与商城图片修复
- 合入前的完整修改另保留于 stash：`c01db0c43afabe4901efb8153f5e4524492d1b1c`，没有删除备份或覆盖工作成果
- 没有提交、推送、创建 PR、执行数据库迁移或修改线上账号

## 导航

电脑端常规入口依次为：

1. 首页
2. 学习世界
3. 讨论区
4. 我的工作台
5. 实验室
6. 小工具工坊
7. PBL计划
8. 问问 Max

活动报名保留为第九个辅助入口，统一标为「活动报名（试用）」
管理员端仍然仅按既有权限显示，在常规入口之后；设置仍在右上角
DOM 顺序与视觉顺序一致，键盘遍历不会跳乱

手机端保留新主分支五项底栏布局：

- 学习菜单：学习世界、实验室、小工具工坊
- 工具菜单：发展端、我的工作台、PBL计划、活动报名（试用）、个人设置、通知中心及按权限显示的管理员端
- 中间创建菜单：发帖、问问 Max
- 修正键盘切换菜单时其他菜单未关闭的问题
- 修正旧全局文字颜色覆盖底栏标签的问题，不改变手机布局

## PBL 规划页

- 路由 `/pbl`；`/pbl.html` 保留查询参数并重定向至规范路由
- 复用发展端现有 `development-construction-bbs-v2.webp` 插图与设计变量，没有生成新图
- 计划时间使用语义化 `time datetime="2027-03"`，目标版本 V2.0；明确「规划中 · 暂未开放」与最终开放以公告为准
- V1.0 规划依据：`99 CodeX/01 平台战略与产品设计/01 平台定位与总体架构/FREE BBS平台定位与V1.0规划-20260728.md`
- 内容采用该文档的自主探索、真实问题与项目实践方向，以及后续 PBL 项目匹配、悬赏流程的规划；不把草案描述成已上线能力
- 不加载发展端的准入/自动跳转脚本，不影响发展端自身 V1.1 / 2026 年 10 月时间表
- 公共施工图角标固定白字，避免亮色模式下被正文颜色覆盖

## 新更新兼容

- 小工具工坊使用已合入的真实页面、后端路由与隔离预览逻辑，没有替换成占位页
- 电路实验室依然从实验室内进入，保留最新手机电路工作区、顺序闯关与排行逻辑
- 保留黄金名片、讨论 @ 提及、发展端准入更新；商城等比图修复同时覆盖黄金名片
- 仅微调导引中活动报名入口的提示；没有新增导引步骤、变更步骤 ID、奖励资格或已完成进度
- 同一共享脚本中旧的裸 location 与字符串拼接改为 window.location / 模板字符串，跳转 URL 与行为不变；相应测试直接运行跳转函数验证结果

## 验证记录

- 电路、导航、PBL、工坊、通知、经济、发展端边界、牧场、字体和页脚相关测试：715 通过，4 项环境依赖集成测试跳过
- `test:onboarding`：258 通过，2 项 MySQL 测试跳过
- 讨论、首页、真实 HTTP 路由、导航相关补充测试：69 通过
- `check-personal-ui-browser.js`：640 组通过（8 页面 × 5 屏宽 × 2 明暗 × 4 字体 × 2 字号），并验证资产说明、历史对话、通知、主题、牧场和实验室入口
- `check-navigation-pbl-browser.js`：桌面顺序、手机五项底栏、PBL 工具菜单归属、键盘菜单互斥、实际页面跳转及文字颜色检查通过
- `check-max-guide-browser.js`：桌面明亮慢响应、手机暗色延迟响应、桌面暗色大字三种场景，各 48 步通过；仅模拟进度与奖励写入
- `check-shop-inspect-images-browser.js`：202 组通过，包含黄金名片在内的 12 类位图商品和仓库详情，无拉伸、横向溢出或非预期写请求
- 本轮涉及的共享脚本和测试 ESLint 无 error；有既有 warning
- 格式检查、无冲突标记、`git diff --check` 通过

这些是本地模拟与代码级检查，不等于生产环境验收
没有安装发展端独立工作区依赖，也没有重跑其完整 TypeScript 构建或真实 MySQL 集成；这部分仍应以 PR CI 和部署后检查为准
各测试组有重叠，不把数量相加当成独立用例总数

## 预览

- `http://127.0.0.1:3133/pbl`：个人空间与导航预览
- `http://127.0.0.1:3134/pbl`：含学习世界、课程与完整导引的模拟预览
- `http://127.0.0.1:3133/tool-workshop`：真实工坊 UI；本地故意禁用 AI 生成/发布，不连接真实服务

下一轮再进行完整区域文案与新手导引 UI 调整
