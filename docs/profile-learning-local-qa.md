# 姓名、昵称及知识学习界面：本地实现与验收

## 范围与基线

- 日期：2026-09-11。本轮基于本地提交 `0620821`，不宣称已获取最新远端 main。
- 本轮不执行 Git 远端操作、分支切换、暂存、提交或合并。修改仍在现有开发目录，供下一次 PR 使用。
- 显示框补齐基于用户已提交的 `9a36836`，所在分支为 `feat/profile-nickname-knowledge-history`；追加修改仍由用户提交、推送。
- 不修改此前商城、头像和课程星球功能，不改活动管理业务。

## 规则与实现

1. 姓名在个人设置只读。`PATCH /api/profile` 不再写入姓名；试图提交不同姓名返回 403。管理员继续在原用户管理入口修改姓名。旧客户端提交未变化的姓名仍可保存简介。
2. 昵称继续使用现有 `username` 字段及 3–64 位字母、数字、下划线规则，同时改变登录用户名；邮箱登录和用户 ID 不变。不新建独立中文昵称字段。
3. 初次自愿改名免费。每次免费改名后，从该次时间起满 **3 个自然月** 恢复一次免费机会，次数不累计。由数据库按 UTC 计算，月底按目标月最后一天处理，不按固定 90 天或浏览器时钟计算。
4. 免费机会不可用时，每次支付 10 磁元。需明确勾选支付确认；付费改名不延后免费时间。原本不合规的历史用户名强制修复仍免费，不消耗自愿改名机会。
5. 同一事务锁定用户行，检查当前昵称、免费资格及余额，更新昵称与余额并写入改名流水。重复昵称、余额不足及写入失败均不应消耗磁元或免费机会；相同昵称重试不重复扣费。并发不同目标通过原昵称校验拒绝过期请求。
6. 改名不保存或清空未提交的个人简介、个人网页。接口结果不确定时先刷新改名信息核对，不自动重试扣费。
7. 课程讨论返回按钮在长帖滚动容器中吸顶，返回时保留帖子列表位置和键盘焦点。面板打开时隐藏右下角悬浮按钮，顶部关闭按钮保留 44px 点击区域，支持 Escape。
8. 对照《FREE BBS平台定位与V1.0规划-20260728》第 572、578、580 行，新增吸顶悬浮的“知识起源”框。按用户后续要求，展开时与正文同宽，随右侧交互区开关、拖动分隔线及屏幕宽度自适应；收起后保留右上小标签。明亮模式为浅紫色，暗色模式为低饱和灰紫色；展开后解释“我们为什么要学习这个知识点？”和“为了解决什么问题，出现了这个知识？”。**内容仅显示“正在开发”**，未填充课程历史、未新增内容编辑接口，后续课程组内容维护另行实施。
9. 框首次显示默认展开，可收起为“知识起源”标签并再次展开，支持键盘与 Escape。展开状态按账号、课程、知识点分别保存在当前浏览器；存储不可用时仍可操作。仅在正文中显示，学习概览、其他工具、课程组编辑器内均隐藏。框不进入课程 Markdown，不改变已有“学习概览 → 开始阅读正文”的入口流程，也不实现规划中的完整继续学习状态恢复。

## 接口及数据库部署

- `GET /api/profile/username`：读取当前昵称、免费资格、下次免费时间、费用及余额，响应禁止缓存。
- `PATCH /api/profile/username`：正常改名提交 `{ username, expectedUsername, allowPaid }`。`expectedUsername` 必须匹配当前值；`allowPaid` 必须为布尔 true 才允许付费。用户身份始终取认证结果，不接受请求体覆盖用户 ID 或费用。
- 新增 `database/migrations/034_username_changes.sql`：仅创建 `username_change_log`，不修改现有用户表。迁移可重复运行；服务启动也会执行同样的建表检查。需部署后端并确认新表存在，再验收改名入口。
- 新表记录用户 ID、旧/新昵称、改名种类、磁元费用和 UTC 时间。它不提供公开历史查询接口。
- 回退时保留已写入的改名记录和余额，不删除新表；若需撤销扣费，应根据流水由管理员核对处理，不自动倒扣或覆盖余额。

## 已完成本地验证

- 新增昵称事务、接口、姓名权限及前端状态测试；覆盖首次免费、付费确认、余额 9/10 边界、重复昵称、流水失败回滚、重复请求、并发旧请求、历史用户名修复、伪造用户 ID、退出登录后的迟到响应与未保存资料保护。
- 整套本地 Node 回归覆盖课程地图、电路、认证、阅读设置、社区、商城头像、活动管理及新功能：73 个测试文件、787 项测试，780 项通过、0 项失败、7 项真实数据库集成测试跳过（包含新增的改名 MySQL 验证）。显示框另含 11 项行为与结构回归，覆盖状态隔离、存储异常和键盘操作。
- 浏览器使用真实页面及脚本、Chrome 无头模式、隔离模拟 API，完成 72 个场景（1440/768/390/320px × 明暗模式 × 9 类检查）；验证改名及资料保存，以及显示框配色、与正文同宽、右侧交互区开关/拖动时同步伸缩、长文上下往返滚动吸顶、收起/展开、刷新恢复、不同知识点隔离、其他工具/编辑器隐藏、大字模式、长帖返回和焦点恢复。没有真实登录、扣费、上传或数据库写入；既有 3000/3001 预览服务未改动。
- ESLint、相关文件格式、脚本语法与 SQL 静态安全检查通过。截屏已检查手机端长帖操作栏及历史框。

常规本地复测：

```bash
npm run test:profile-learning
npm run test:course-maps
npm run test:shop-settings
npm run test:community
npm run lint
```

## 手动查看显示框与提交

用户本机已确认可用的完整预览启动方式保持不变：在开发仓库执行 `bash scripts/start-local.sh`，保留该终端，打开 `http://127.0.0.1:3000/login`。脚本加载 `backend/.env`（没有时加载 `envs.sh`），启动 3000 前端和 3001 后端；当前配置指向本机 MySQL 的 `free_bbs` 库。它不属于隔离模拟测试，后端会检查/创建表，实际改名也会写入本地数据库。不要改为连接生产数据库进行测试。

从学习世界进入课程知识点，点击“开始阅读正文”查看右上方浅紫色“知识起源”框；长文滚动时框保持可见，收起、刷新、切换明暗模式后再检查。若服务已运行，修改静态页面后只需强制刷新，不必重复启动服务；Node 回归可以在另一个 Git Bash 窗口运行。

在 GitHub Desktop 确认开发仓库及 `feat/profile-nickname-knowledge-history` 分支，核对本次文件后追加提交，建议提交说明为 `fix: 完善知识起源悬浮框与主题适配`。随后 Push origin；若该分支已有 PR，会更新同一 PR，无需重复创建。

## 尚需真实数据库联调

本轮未连接真实 MySQL。模拟事务测试不能替代真实行锁、唯一约束、月底日期算法、迁移权限及故障恢复验证；涉及扣费，正式发布前应完成该项。

已提供显式开启的隔离集成测试：为专用测试 MySQL 配置 `USERNAME_TEST_MYSQL_HOST`、`USERNAME_TEST_MYSQL_PORT`、`USERNAME_TEST_MYSQL_USER`、`USERNAME_TEST_MYSQL_PASSWORD`，然后设置 `RUN_USERNAME_INTEGRATION=1` 执行 `node --test backend/username-changes.test.js`。

此测试只会创建随机命名的 `freebbs_username_test_<随机值>` 数据库，结束后删除它自己创建的库。使用具备创建/删除测试库权限的专用账号，**不要使用生产数据库账号**。它验证重复迁移、并发免费资格、扣费/重复提交、唯一约束回滚、月末及闰年边界。

正式环境还应验证：改名后新昵称和邮箱登录、管理员修改姓名、普通用户无法绕过限制，以及两台设备同时改名时只执行有效的一次。
