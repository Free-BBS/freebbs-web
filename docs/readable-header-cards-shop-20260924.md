# 页头、日程可读性与商城实物一致性 · 2026-09-24

本轮继续在 `fix/header-currency-search-20260924` 本地工作树修改，保留已有课程、导引及商城变更，未提交或推送。

## 页头标题

之前的测量和断言采用被裁剪后的标题宽度，误把省略号当作布局正常。现在按完整文本的实际字体测量：标题优先，搜索靠右放在账户区左边；空间不够先收成搜索图标、收起非必要用户名详情，超长标题换行并扩展标题占位。

真实 Chrome 验证 456 组布局，覆盖电路实验室等 14 页、8 种宽度、明暗和标准/大字号，以及长标题与游客。检查自然文字宽高、无省略、无遮挡及搜索点击/快捷键，不再只验证裁剪后的矩形。

报告：`C:/Users/yc-z2/AppData/Local/Temp/freebbs-header-qa-AbKeLw/report.json`。

## 日程卡片

普通日程和固定课程卡片只优先呈现名称与地点/备注，保留换行，时间在点击后的编辑/只读详情和无障碍标签中可见。既有网络学堂作业的截止时间与完成控件不改动。

卡片起点仍按真实时间定位，高度至少容纳完整文字；按卡片视觉占位分列，避免短事件、相邻事件、真实重叠互相遮挡。日末有文字超出时，七列同步补齐底部高度；日历内部可以横向滚动，不撑出页面。明确告知卡片高度不代表精确时长，不改动原始日程时间或保存数据。

- 新增浏览器脚本 `npm run test:workbench:cards:browser`：32 组（桌面/手机、明暗、四种字体、标准/大字）通过；最终报告为 `C:/Users/yc-z2/AppData/Local/Temp/freebbs-workbench-card-layout-ZR0Rnm/report.json`。
- 覆盖 21:02–22:02、5 分钟、长名称、多行备注、相邻/重叠、23:59 日末及点击查看准确时间。
- 备注与显示时间旧浏览器回归、固定课程与批量安排浏览器回归均通过。

报告：`C:/Users/yc-z2/AppData/Local/Temp/freebbs-workbench-card-layout-ZR0Rnm/report.json`。

## 商城展示原则

头像框、铭牌、主页主题必须复用真实佩戴/应用组件与样式，不用概念插画冒充实际商品。Max 复用现有牧场羊几何与配色，不改变牧场行为。商品价格、限购、使用条件与奖励不在本轮调整范围。

小鱼用内置 image_gen 编辑为普通银灰饲料鱼：去掉观赏鱼大尾鳍、艳丽配色和夸张表情，保留精细手绘质感。使用已有转换脚本机械缩放为 512×512 透明 WebP，约 27 KiB，不引入新的运行依赖。

- 小鱼交付资源：`public/assets/shop/max-cartoon-v1/fish.webp`。
- 完整提示词与生成来源：`docs/shop-feeding-fish-art-20260924.json`。

商城、仓库和端详窗口统一使用同一预览入口；修复公共铭牌文字颜色被全局样式覆盖的问题，实际佩戴与预览一起保持原有紫色文字。手机/窄屏页底增加安全留白，避免最后一排端详按钮被悬浮导引挡住。

- 商城布局 Chrome：20 种尺寸、明暗与字号组合通过，报告 `C:/Users/yc-z2/AppData/Local/Temp/freebbs-shop-presentation-azaK2o/layout.json`。
- 真实样式 Chrome 对照：2 种尺寸 × 2 种明暗 × 商城/仓库 × 6 款商品及其详情，共 96 次对照通过。直接与实际个人主页的边框、阴影、文字色、背景和 Max 四肢几何比较，另验证默认中心点击、移动端页底留白及羊蹄完整可见。
- 对照报告与截图：`C:/Users/yc-z2/AppData/Local/Temp/freebbs-shop-cosmetics-f4mvi5/report.json`。0 页面错误，模拟账号前后状态一致。

浏览器验证仅使用本地回环服务、独立临时 Chrome 与模拟数据，不操作真实账号；不构成真实网络学堂、MySQL 或 Agent 上线验收。

## 兼容回归

- 工作台套件：134 项通过。
- 商城与设置套件：214 项通过；真实装扮/牧场复用专题：3 项通过。
- 新手导引套件：257 项通过、2 项真实数据库测试按环境跳过。
- 完整导引 Chrome：桌面浅色和手机深色各 48 步通过，课程接口延迟 3.5/6.5 秒仍可走完；0 页面异常、0 原生转场异常。
- 导引报告：`C:/Users/yc-z2/AppData/Local/Temp/freebbs-guide-browser-fdMLM4`。
- 本轮文件 Prettier 和 `git diff --check` 通过。ESLint 检出 `public/app.js` 已有 9 处错误，已用 `git show HEAD:public/app.js` 对照确认同样存在于基线（全局 `location` 与字符串拼接规则），本轮未新增，也未顺带修改无关代码。

本轮继续使用本地预览 `http://127.0.0.1:3126/workbench` 和 `http://127.0.0.1:3126/electromagnetic`，由内存模拟接口提供数据，不是线上部署。

## 商城视觉收敛与货币字体复核

装扮区仍直接复用真实头像框、铭牌和主页主题，商品卡增加细描边、双层内框、低强度冷暖光晕和轻微悬浮层次，使真实效果更完整而不伪造另一套商品样式。激光器改为哑光黑灰与暖白的小型实验仪器，取消可见光束、红色玩具外壳和金色装饰。

收藏与消耗品统一降低饱和度、发光和仪式感：神秘碎片去掉环绕碎片与霓虹裂纹；眼镜去掉彩虹反光与星光；共振器、感应环和幸运硬币改成旧实验室器材/学术纪念品；微分器改为朴素的教学仪器；福袋改为棉布束口袋。所有资源保留透明边缘、512 × 512 WebP 和精细手绘质感。

- 8 个重绘素材的生成来源、完整提示词与最终路径：`docs/shop-quiet-art-20260924.json`（内置 image_gen 编辑模式）。
- 商城 Chrome：20 种尺寸、明暗与字号场景通过，0 页面错误、0 业务写入；报告与截图：`C:/Users/yc-z2/AppData/Local/Temp/freebbs-shop-presentation-cQ53MN`。
- 装扮一致性复测：商城、仓库、端详窗口在桌面/手机与明暗模式下，共 96 次与真实个人主页的逐项样式对照通过；报告：`C:/Users/yc-z2/AppData/Local/Temp/freebbs-shop-cosmetics-1XpSbu/report.json`。
- 商城与设置回归：214 项通过。
- 页头货币数字复核：14 个页面、8 种宽度、明暗模式与标准/大字号共 456 个布局通过。电元、磁元、热力的数字均使用相同字体、字号、700 字重、行高和 `tabular-nums`；例如商城桌面标准字号均为 14.4px。报告：`C:/Users/yc-z2/AppData/Local/Temp/freebbs-header-qa-1EIAX1/report.json`。
