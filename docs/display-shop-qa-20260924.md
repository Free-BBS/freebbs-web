# 页头、商城与知识页显示回归 · 2026-09-24

## 分支与范围

- 分支：`fix/header-currency-search-20260924`。
- 最终复查已快进到 `origin/main`：`89036ea`（`match challenge circuit editing tools`），工作区修改无冲突保留。
- 本轮仅在独立 worktree 修改；没有提交、推送、合并或生产环境写入。
- 商品目录只替换 17 个启用商品的图片路径；已与 HEAD 比较，其他字段完全不变。不修改价格、资产数量、奖励、道具有效期、支付和库存逻辑。

## 已实现

1. 电元、磁元、热力的数字使用相同字体、字号、字重、行高与等宽数字。电元和磁元用 button，热力用 span，之前仅设置粗体仍会继承不同字号。
2. 搜索入口按实际渲染的页头标题宽度布局，包含页面 CSS 自定义的面包屑、字体切换和大字号。空间不足时收为搜索图标；标题为搜索、用户区及手机主题按钮预留空间。
3. 电磁场增加暖色欢迎区、仓库入口、四类商品标牌与锚点；17 件商品分别采用独立卡通插画，与 Max 保持画风而不强制同色。商品图底、间距、价格与操作区域更统一，适配明暗模式、手机与大字。
4. 新商城样式只限于 `body.electromagnetic-page`，不修改仓库或共用购买弹窗的规则；保留原有导览目标、商品节点与操作属性。
5. 修复讨论详情回帖的透明背景重置覆盖激光光晕；手机作者链接允许换行，避免铭牌被逐字挤成竖列。
6. 知识起源保留 sticky，但顶部偏移由 128px 改为 0，贴住视口顶部。右侧学习面板记忆开关与 Max/课程讨论标签，切换知识点和刷新后恢复；不保存对话内容，存储受限时安全回退。

## 插画资源与加载

- 使用内置 imagegen 逐件生成，参考现有 Max；完整提示词、来源文件名见 `docs/shop-cartoon-art-20260924.json`。
- 交付目录：`public/assets/shop/max-cartoon-v1/`，17 张透明 WebP，均为 512×512。原 SVG 保留，不覆盖。
- `scripts/prepare-shop-art.js` 只做透明背景保留、等比缩放和 WebP 压缩，默认不覆盖已有版本文件。
- 图片使用 lazy loading、异步解码和固定尺寸；单图小于 160 KiB，全套小于 2 MiB，由 `scripts/shop-art.test.js` 校验。
- 浏览器测试逐张滚入视口并完成解码后才判断图片是否缺失；340 次方形图框测量均无宽高偏差。

## 验证

所有浏览器测试均使用临时 Chrome 配置、本地 loopback 预览及内存账号，不连接官网或真实数据库。

| 验证项                          | 结果                                                                                                                                           |
| ------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| 页头浏览器回归                  | 456 组通过：14 页面 × 8 宽度 × 2 明暗 × 2 字号，另加长标题和游客场景；验证完整标题、搜索点击、Escape 与 Ctrl+K                                 |
| 页面横向溢出                    | 上述页头快照中无整页横向溢出；手机资产栏原有横向滚动仍保留，不等于所有资产都必须在窄屏首屏同时显示                                             |
| 商城浏览器回归                  | 20 组通过：5 宽度 × 2 明暗 × 2 字号；检查分类跳转、17 件商品图片与边界、详情弹窗开关/滚动/焦点恢复；全部拦截外部与写请求，完整内存账号快照不变 |
| 完整导览浏览器回归              | 桌面浅色与手机深色各 48 步通过；课程接口分别延迟 3.5 / 6.5 秒；只允许本地导览进度与一次性导览奖励写入                                          |
| 商城/页头/搜索/讨论相关测试     | 165 项通过                                                                                                                                     |
| `npm run test:shop-settings`    | 214 项通过，0 跳过                                                                                                                             |
| `npm run test:onboarding`       | 257 项通过，2 项真实 MySQL 集成测试因未启用环境跳过                                                                                            |
| `npm run test:profile-learning` | 51 项通过，1 项真实 MySQL 测试跳过                                                                                                             |
| 讨论激光与铭牌浏览器回归        | 92 个激光样式快照、4 个作者区快照均通过，普通/过期道具、高对比度与减少动态模式正常                                                             |
| 知识页浏览器回归                | 8 组通过：1440/768/390/320px × 明暗；验证吸顶 top=0、长文、收展、工具栏关闭后切点、讨论标签刷新恢复                                            |

本地预览：<http://127.0.0.1:3125/electromagnetic>。该链接需要本机预览进程保持运行，不是线上部署。3125 使用新的内存目录快照，避免旧预览进程继续提供旧插画路径。

浏览器脚本使用已有 Puppeteer 安装，不需要在项目安装新依赖：

```powershell
$env:PUPPETEER_MODULE = 'D:/E For Study/Doctor/101 团委辅导员/电子系自主学习平台建设/99 CodeX/98 临时文件/guide-browser-runtime-20260924/node_modules/puppeteer-core'
$env:CHROMIUM_EXECUTABLE = 'C:/Program Files/Google/Chrome/Application/chrome.exe'
npm run test:header:browser
npm run test:shop:browser
npm run test:onboarding:browser
npm run test:discussion:laser:browser
npm run test:knowledge:browser
```

## 本机浏览器证据

以下均为本机临时目录，不纳入生产资源：

- 页头：`C:/Users/yc-z2/AppData/Local/Temp/freebbs-header-qa-1EIAX1`。
- 新插画商城：`C:/Users/yc-z2/AppData/Local/Temp/freebbs-shop-presentation-cQ53MN`，含 `layout.json` 及明暗、大小字截图。
- 最新完整导览：`C:/Users/yc-z2/AppData/Local/Temp/freebbs-guide-browser-fdMLM4`。
- 激光与铭牌：`C:/Users/yc-z2/AppData/Local/Temp/freebbs-laser-qa-iQZaI1/report.json`。原基线失败断言修复后全部通过。
- 知识起源和面板偏好：`C:/Users/yc-z2/AppData/Local/Temp/freebbs-knowledge-history-qa-bVfOYT/report.json`。

## 成就系统：仅讨论，未实现

- 完成导览：保留每账号一次 10 电元 + 10 磁元，可增加纪念徽章，不重复发币。
- 第一张计划表：首次保存日程，建议一次性 2 磁元。
- 可选的个人装扮：首次设置头像、简介或装扮，建议一次性 2 磁元；不要求填写真实隐私信息。
- 参与讨论、累计不同日期访问、首次验证成功的电路挑战：优先纪念徽章，复用既有互动、签到、挑战奖励。
- 牧场互动可做可选纪念任务，不把购买 Max 设为必须完成的新手条件。
- 不以发帖数量、AI 调用次数、重复点击或消费金额激励刷量；首次保存空电路也不算掌握电路知识。
- 以后实现时按账号 + 固定成就 ID 在服务端保证一次性，兼顾有可靠历史证据的老用户。

## 边界

这不是所有账号、设备、业务数据和生产服务的全面认证。未测试真实 AI、真实数据库并发及线上部署。导览中的活动回执空数据场景仍按现有逻辑等待约 10 秒后给出空态，不代表所有步骤瞬间就绪。所有生产变更目前只在本地工作树，尚未提交或推送。
