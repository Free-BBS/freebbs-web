# 商城、头像、课程星球与活动分页本地验证

验证日期：2026-09-11。开发分支为 `feat/shop-avatar-course-planets`，已由维护者将包含 #81 的 `origin/main`（`5380de0`）合入，当前合并提交为 `9d29c81`。下述验证包含该提交之上的分页修复及测试适配；本轮测试未访问项目远端、生产 API 或现有数据库，也未替维护者暂存、提交或推送。

## 本轮范围

- 商城和仓库采用内容自适应卡片，清晰展示持有量、价格及详情；完善长说明弹窗的滚动、键盘关闭、焦点返回和购买反馈。不改变商品定价或交易规则。
- 设置页头像支持选择、预览、确认、取消和失败重试，上传不覆盖尚未保存的个人资料。
- 头像后端核验真实图片内容，限制大小及解码尺寸，统一生成最长边 512px 的静态 WebP；GIF 取首帧。事务失败与文件清理优先避免删除仍在使用的头像。
- 三门课程的小星球换成与岛屿相近的游戏风插画，保留原课程入口和旋转逻辑。素材与生成提示词见 [课程星球素材说明](course-planet-assets-v2.md)。
- 保留主分支的用户名、通知、课程上传、活动入口和学习世界视觉调整；小屏课程标签随轨道位置内移，透明素材使用轮廓阴影。
- 活动管理列表与报名详情分别使用请求版本号；翻页、刷新、切换活动或关闭详情后，旧响应和旧导出回调失效。过期普通错误不覆盖当前提示，401/403 仍清除管理员资料，保留服务端导出权限复核。
- 补充 11 项活动竞态回归，并将 `test:surveys` 接入 CI。修正 Windows CRLF 下部分测试提取源码的边界，以及课程上传测试的 Python 解释器和文件句柄兼容性；不改变 Max、讨论区或电路业务逻辑。

## 自动化验证

使用 Node.js 24.20.0，依赖按锁文件安装。测试子进程不继承数据库、邮件或 Agent 凭据；网络测试仅使用本机临时端口和模拟服务。

| 检查                                                                         | 结果                                |
| ---------------------------------------------------------------------------- | ----------------------------------- |
| CI 所列 10 套 Node 回归，加 `test:aichat-navigation`，去重后 70 个测试文件   | 共 751 项：745 通过，0 失败，6 跳过 |
| `node --test scripts/activities-admin.test.js`（已计入上述总数）             | 18 项通过，包含 11 项新增竞态回归   |
| `python -B backend/course-upload-client.test.py`（使用本机实际 Python 路径） | 7 项通过                            |
| ESLint、相关 JS/HTML/CSS/JSON/Markdown 格式检查                              | 通过                                |
| CI Bash 语法及 `scripts/assert-safe-sql.sh`                                  | 通过                                |
| Sharp 实际 WebP 编码、ExcelJS / JSZip 加载                                   | 通过，无需额外批准安装脚本          |

Node 套件包括 `test:course-maps`、`test:circuits`、`test:public-pages`、`test:auth`、`test:shop-settings`、`test:admin-users`、`test:discussion-markdown`、`test:agent-surfaces`、`test:community`、`test:surveys` 和 `test:aichat-navigation`。以上按测试文件去重统计，不重复计入认证与社区共用测试。

跳过的 6 项均需要独立真实 MySQL：电路示例、电路存储、社区整合、通知、白名单事务，以及活动生命周期/并发/迁移。头像测试包含真实 Sharp 解码和编码，但数据库操作使用替身；不能据此声称完成真实数据库联调。活动本地代理测试已运行，覆盖自定义头像资源、活动 API 与页面路由。

新增竞态用例先在旧实现上复现失败，再验证修复后的结果；同时检查普通错误、权限错误、失败重试、旧导出回调及不同活动的响应交错。

## 浏览器验证

自动检查使用 Chrome 无头浏览器、独立临时端口和内存模拟接口，阻止外部请求；不访问原 `3000/3001`，不上传真实用户头像或购买物品。供维护者手动预览的固定端口仍为 `3106`。

- 商城、仓库、设置和学习世界在 320、390、768、1440px 宽度及明暗主题下，共 56 个场景通过，未发现页面横向溢出或页面脚本异常。
- 详情可在小屏内滚动；关闭按钮保持可访问；Escape 关闭后焦点回到入口。
- 设置页验证头像预览、首次失败、重试成功，以及未保存简介和登录令牌保持不变。
- 仓库为空时显示明确空状态和商店入口。
- 三门课程验证真实入口、透明素材、六个轨道位置及标签边界，四套字体选择可正常应用。
- 活动管理实际页面与脚本使用模拟管理员/API，额外 32 个浏览器场景通过：列表乱序、翻页后的旧详情、不同活动详情交错、关闭后的迟到导出。覆盖同样的四种屏宽及明暗主题，不发生实际下载或外部请求。

合计 88 个浏览器场景通过。此处仅为当前 Chrome 的响应式和交互检查，未声称覆盖所有浏览器、真实手机或真实数据库；课程轨道检查启用了减少动态效果。

## 复测入口与限制

运行 `node scripts/preview-shop-settings.js` 后打开 <http://127.0.0.1:3106/>。该服务仅监听本机；右下角隔离测试工具可切换长内容、空仓库和上传失败场景。模拟头像仅存于内存，未实现的写接口一律拒绝。

现有 `3000/3001` 服务未被重启。正式发布前应在独立测试环境启动实际后端，使用测试账户验证头像持久化，并补齐活动迁移交错、并发报名与抽签等真实 MySQL 路径。数据库或文件系统异常时可能保留待清理文件，避免误删有效头像；本轮未新增数据库迁移，也未修改 #81 的数据库或抽签算法。
